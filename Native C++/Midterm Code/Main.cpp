#include <iostream>
#include "GameObject.h"

int main()
{
    /*GameObject gameObject;

    gameObject.SetID(1);
    gameObject.SetTime(100, 200, 300);

    std::cout << "The Game Object's ID is       : " << gameObject.GetID() << std::endl;
    std::cout << "The Game Object's Time is : (" << gameObject.GetTime().hours 
    << ":" << gameObject.GetTime().minutes << ":" << gameObject.GetTime().seconds 
    << ")" << std::endl;*/
}